LINE_CHANNEL_ACCESS_TOKEN=replace_with_line_messaging_api_token
LINE_TO=replace_with_user_group_or_room_id
# Optional when the repository is private. Must be a public HTTPS directory
# that exposes latest.png, for example https://example.com/gold/generated
PUBLIC_IMAGE_BASE_URL=
